# ==========================================
# INFRASTRUCTURE CONFIGURATION
# ==========================================
# The public domain name for your Netbird instance
DOMAIN=netbird.example.com

# Public IP addresses of your server (Required for Coturn)
PUBLIC_IP=1.2.3.4
# PUBLIC_IPV6=2001:db8::1  # Optional

# ==========================================
# NETBIRD CORE CONFIGURATION
# ==========================================
# Management API Endpoints
NETBIRD_MGMT_API_ENDPOINT=https://netbird.example.com
NETBIRD_MGMT_GRPC_API_ENDPOINT=https://netbird.example.com:443

# Setup Keys (Generate strong random strings)
NETBIRD_SETUP_KEY=

# ==========================================
# ZITADEL (IDENTITY PROVIDER)
# ==========================================
# Port exposed to Caddy/Reverse Proxy
ZITADEL_EXTERNAL_PORT=443

# Masterkey for Zitadel encryption (Must be at least 32 chars)
# Generate via: tr -dc A-Za-z0-9 </dev/urandom | head -c 32
ZITADEL_MASTERKEY=change_me_to_a_secure_32_char_string

# Database Credentials
POSTGRES_USER=zitadel
POSTGRES_PASSWORD=change_me_securely
POSTGRES_DB=db_prod
POSTGRES_HOST=netbird-postgres

# ==========================================
# CROWDSEC SECURITY
# ==========================================
# Optional: Key to enroll this instance in CrowdSec Console
CROWDSEC_ENROLL_KEY=

# Firewall Bouncer API Key
# 1. Start CrowdSec container first
# 2. Run: docker compose exec crowdsec cscli bouncers add firewall-bouncer
# 3. Paste the key here
CROWDSEC_BOUNCER_KEY=
